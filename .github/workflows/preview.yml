name: Expo Preview

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  publish-preview:
    runs-on: ubuntu-latest

    steps:
      - name: 🏗 Depoyu Çek
        uses: actions/checkout@v4

      - name: 🧹 Temizlik Yap (Eskileri Sil)
        run: rm -rf node_modules package-lock.json package.json eas.json

      - name: 🏗 Node.js Kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 👇 İŞTE ÇÖZÜM BURASI: package.json'ı EN GÜNCEL HALİYLE YENİDEN YAZIYORUZ
      - name: 🔥 Fix Versions (Upgrade to SDK 52)
        run: |
          echo '{
            "name": "eminxtream",
            "version": "1.0.0",
            "main": "node_modules/expo/AppEntry.js",
            "scripts": {
              "start": "expo start",
              "android": "expo start --android",
              "ios": "expo start --ios",
              "web": "expo start --web"
            },
            "dependencies": {
              "@react-native-async-storage/async-storage": "1.23.1",
              "@react-navigation/bottom-tabs": "^6.5.11",
              "@react-navigation/native": "^6.1.9",
              "@react-navigation/stack": "^6.3.20",
              "expo": "~52.0.25",
              "expo-av": "~15.0.2",
              "expo-keep-awake": "~14.0.2",
              "expo-status-bar": "~2.0.1",
              "react": "18.3.1",
              "react-native": "0.76.6",
              "react-native-safe-area-context": "4.12.0",
              "react-native-screens": "~4.4.0"
            },
            "devDependencies": {
              "@babel/core": "^7.20.0"
            },
            "private": true
          }' > package.json

      - name: 📦 EAS CLI Yükle
        run: npm install -g eas-cli

      - name: 📦 Bağımlılıkları Yükle (Güncel)
        run: npm install

      # Projeyi Tekrar Bağla
      - name: 🔗 Projeyi Eşleştir (Link)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas init --id 7b7866e2-688e-4e87-af97-88213b2670d4 --non-interactive

      # Güncellemeyi Gönder
      - name: 🚀 Expo'ya Gönder (Update)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas update --branch preview --message "SDK Upgrade Fix" --non-interactive
